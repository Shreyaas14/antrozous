# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Attachment blob store + in-memory relay backup (runtime state, not source)
blobs/
messages.json

# Personal, per-user Claude Code settings (agent identity, etc.)
.claude/settings.local.json

# Per-project antrozous identity (runtime state, never source)
.antrozous/

# Xcode build state for the enclave helper
helper/AntrozousHelper.xcodeproj/xcuserdata/
helper/AntrozousHelper.xcodeproj/project.xcworkspace/
